"""watertwin core library (API-side domain logic).

Currently exposes :mod:`watertwin.calculations`, the lightweight analytical
reference for reverse-osmosis performance used by the API for quick estimates
and as an independent cross-check against the ``treatment-sim`` service.
"""
