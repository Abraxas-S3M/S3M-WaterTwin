# edge-gateway service dependencies (pinned).
# An outbound-only, read-only OT edge collector: reads real OT feeds (reusing
# the shared read-only ot_ingestion.sources adapters), validates data quality,
# buffers to a local ENCRYPTED store-and-forward queue, and pushes canonical
# telemetry OUTBOUND to the watertwin-api ingest endpoint. It never binds an
# inbound listener.
pydantic==2.10.4
# Outbound HTTP client for the push path (no server component).
httpx==0.28.1
# Encrypt-at-rest for the local SQLite store-and-forward buffer (Fernet:
# AES-128-CBC + HMAC-SHA256 authenticated encryption).
cryptography==44.0.1
# Read-only OT connectors (shared, from packages/ot_ingestion). Used strictly as
# read-only clients; no control-write path is ever exercised (enforced by the
# read-only boundary-guard test that scans the shared source package).
#   asyncua  -- OPC UA CLIENT only (LGPL-3.0; consumed unmodified via its wheel).
#   pymodbus -- Modbus client using read function codes only (BSD-3-Clause).
asyncua==1.1.6
pymodbus==3.7.4

# Test-only (kept here so the container image can self-test):
pytest==9.1.1
