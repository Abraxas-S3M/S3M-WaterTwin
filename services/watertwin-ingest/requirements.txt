# watertwin-ingest service dependencies (pinned).
# Customer-document TEXT EXTRACTION only. Both parsers read a document's text
# layer/paragraphs; neither executes active content (no PDF JavaScript, no
# embedded-file extraction, no form evaluation, no Word macros).
#   pypdf       -- PDF text-layer extraction only (BSD-3-Clause).
#   python-docx -- .docx paragraph/heading text extraction (MIT).
pypdf==5.1.0
python-docx==1.1.2

# Test-only (kept here so the container image can self-test):
pytest==8.3.4
# Staged-file data intake with optional AI-assisted analysis. Advisory /
# read-only: it analyzes staged files and produces a reviewable diff of
# UNACCEPTED proposed changes; it never commits, approves, or issues a control
# write. Versions match services/watertwin-api so the shared dependency register
# and SBOMs stay consistent.
# Pinned fastapi to 0.139.2 to match watertwin-api (pulls a fixed starlette
# >=1.3.1; earlier releases carry advisories flagged by pip-audit).
fastapi==0.139.2
uvicorn[standard]==0.34.0
pydantic==2.10.4
# Outbound HTTP client used only to submit advisory analysis requests to S3M-Core.
# Advisory, read-only file-intake service. It exposes an inbound HTTP API for the
# dashboard and makes OUTBOUND calls only to watertwin-api's configuration
# lifecycle. It never connects to, or writes to, any OT system.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
# Outbound HTTP client used by HttpConfigClient to reach watertwin-api.
httpx==0.28.1
# A hardened, tenant-isolated file/document ingestion service. It is the hostile-
# input firewall in front of the platform: every parser runs behind malware scan,
# archive/zip-bomb limits, XXE/XSLT-safe XML, CSV-injection-safe export, a
# resource-capped sandbox, per-tenant quotas, strict tenant isolation and a
# tamper-evident audit chain. It is read-only to OT (deny-all worker egress) and
# has NO control-write path.
# Versions match services/watertwin-api so the shared register + SBOMs stay
# consistent.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
# Hardened XML parsing: forbids DTDs, external entities and entity expansion
# (XXE / billion-laughs). Pure-python, no native code.
defusedxml==0.7.1

# Test-only (kept here so the container image can self-test the controls):
pytest==8.3.4
httpx==0.28.1
# Sandboxed customer-file ingestion: parses an uploaded EPANET .inp inside a
# hardened, network-isolated worker, reconciles it against the canonical config
# fetched from watertwin-api over HTTP, and proposes changes for human review.
# It is read-only to the canonical model and to OT; no control-write path exists.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
# Multipart form parsing for file uploads (FastAPI UploadFile).
python-multipart==0.0.20
# Outbound HTTP client used by the reconciler to fetch the canonical config from
# watertwin-api (no database access from this service).
httpx==0.28.1
# Safe XML handling: uploads are untrusted; an XML-looking payload is parsed with
# defusedxml purely to detect and reject external-entity / DTD (XXE) attacks.
# Bulk file-import staging service: parses historian time-series exports
# (.csv/.parquet) and customer geospatial layers (.geojson/zipped shapefile),
# writes them to a staging area, and emits human-approval proposals. It is
# read-only with respect to OT/PLC/SCADA and never promotes an analytic from
# preliminary to calibrated. Versions match the rest of the monorepo where they
# overlap so the shared dependency register / SBOMs stay consistent.
pydantic==2.10.4

# Streamed Parquet reading (record-batch iteration bounds peak memory).
pyarrow==25.0.0
# Pure-Python shapefile reader (streams shape-by-shape via iterShapeRecords).
pyshp==3.1.4
# Coordinate reference system handling + reprojection to the platform CRS.
pyproj==3.7.2
# Hardened XML parsing (DTDs / external entities forbidden -> no XXE surface).
defusedxml==0.7.1

# Test-only (kept here so the container image can self-test):
pytest==8.3.4
#
# Independently deployable, OPTIONAL intake service. It receives customer files,
# stores them immutably (content-addressed, write-once), scans them structurally,
# and tracks them through a status lifecycle. It has NO direct database
# connection to the canonical store and NO OT network access: it talks to
# watertwin-api over the same authenticated HTTP API a human uses.
fastapi==0.139.2
uvicorn[standard]==0.34.0
pydantic==2.10.4
# Outbound HTTP client used to talk to watertwin-api (audit append). No OT reach.
httpx==0.28.1
# Multipart parsing for streamed file uploads (FastAPI UploadFile / Form).
python-multipart==0.0.20
# Identity / RBAC: JWT bearer validation against Keycloak (JWKS, RS256), reusing
# the same OIDC model as watertwin-api.
pyjwt[crypto]==2.13.0
# Advisory service-event bus (packages/watertwin_events). NATS client only; the
# bus is advisory/notification-only and degrades gracefully when the broker is
# down. Never carries a control command.
nats-py==2.15.0
# Observability: structured JSON logs + correlation ids (shared
# watertwin_observability package). Prometheus /metrics + OpenTelemetry traces.
prometheus-client==0.25.0
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-http==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
# Templated spreadsheet ingestion for equipment specifications, OT tag mappings
# and lab methods. This service is READ-ONLY to OT: it parses uploaded CSV/XLSX
# files into a reviewable diff and never connects to, or writes to, any
# OT/SCADA/PLC/OPC UA/MQTT system.

# Shared canonical models + engineering plausibility ranges (from packages/).
pydantic==2.10.4

# Spreadsheet reading. openpyxl is used strictly in read_only + data_only mode:
# it reads cached cell VALUES only and cannot execute VBA/macros. (MIT licensed.)
openpyxl==3.1.5

# CSV character-encoding detection with an explicit fallback. Pure-python, MIT.
charset-normalizer==3.4.7

# Test-only (kept here so the container image can self-test):
pytest==9.1.1
